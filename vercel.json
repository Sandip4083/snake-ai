{
  "version": 2,
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/scores.js": {
      "maxDuration": 10
    }
  },
  "routes": [
    {
      "src": "/api/scores(.*)",
      "dest": "/api/scores.js"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
